import { CommandUtil } from "../../../types/command";

export const projects: CommandUtil = {
	description: "View my portfolio of projects",
	execute: () => "Here are some of my notable projects: (to be added)",
};
